import { Preset } from './presets';
export declare function applyPresetOnCanvas(canvas: HTMLCanvasElement, ...presets: Preset[]): void;
export declare function applyPresetOnImage(image: HTMLImageElement, ...presets: Preset[]): Promise<Blob | null>;
export declare function applyPresetOnImageURL(url: string, ...presets: Preset[]): Promise<Blob | null>;
export * from './presets';
