/**
 * Apply grayscale effect on the image
 */
export declare function grayscale(): (pixels: ImageData) => ImageData;
/**
 * Apply sepia effect in the image
 *
 * @param adjustment - From 0 (unchanged) to 1 (sepia)
 */
export declare function sepia(adjustment: number): (pixels: ImageData) => ImageData;
/**
 * Apply brightness effect on the image
 *
 * @param adjustment - From -1 (darker) to 1 (lighter). 0 is unchanged.
 */
export declare function brightness(adjustment: number): (pixels: ImageData) => ImageData;
/**
 * Apply saturation effect on the image.
 *
 * @param adjustment - From -1 (desaturated) to positive number. 0 is unchanged.
 */
export declare function saturation(adjustment: number): (pixels: ImageData) => ImageData;
/**
 * Apply contrast effect on the image
 *
 * @param adjustment - From -1 to 1
 */
export declare function contrast(adjustment: number): (pixels: ImageData) => ImageData;
/**
 * Apply color filter effect on the image. It adds a slight color overlay.
 *
 * @param rgbColor - [r, g, b, adjustment] tuple
 */
export declare function colorFilter([r, g, b, adjustment]: [number, number, number, number]): (pixels: ImageData) => ImageData;
/**
 * Adjust RGB of the image
 *
 * @param adjustment - [r, g, b] tuple
 */
export declare function rgbAdjust([r, g, b]: [number, number, number]): (pixels: ImageData) => ImageData;
